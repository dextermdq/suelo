[build]
command = "pnpm build"
publish = "dist"

[[redirects]]
from = "/*"
to = "/index.html"
status = 200

[context.production]
  environment = { NODE_ENV = "production" }

# Deshabilitar inyección de Netlify
[build.environment]
  NETLIFY_USE_RUN_CLI = "false"
